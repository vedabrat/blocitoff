
<% if @item.user == current_user %>
  <% @items.each do |i| %>
  <div class=form-group>

    <%= i.name  %>
    <%= link_to "Delete", @item, method: :delete, class: 'glyphicon glyphicon-ok' %>

  </div>

  <% end %>
<% end %>
